info:
  name: Fetch User
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/users/"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
