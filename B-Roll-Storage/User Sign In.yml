info:
  name: User Sign In
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/auth/signin"
  body:
    type: json
    data: |-
      {
        "email": "admin@gmail.com",
        "password": "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
