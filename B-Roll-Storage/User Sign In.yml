info:
  name: User Sign In
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/auth/signin"
  body:
    type: json
    data: |-
      {
        "email": "dk@gmail.com",
        "password": "thingy123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
