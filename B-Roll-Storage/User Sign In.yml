info:
  name: User Sign In
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/auth/signin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
