info:
  name: Update Media
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{base_url}}/medias/69ef7908ceaf0bba609e1ab7?operation=add"
  params:
    - name: operation
      value: add
      type: query
  body:
    type: json
    data: |-
      {
        "title": "Haobin's Only",
        "tags": ["haobin", "yuri", "yaoi"]
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
