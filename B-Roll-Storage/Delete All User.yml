info:
  name: Delete All User
  type: http
  seq: 15

http:
  method: DELETE
  url: "{{base_url}}/users/"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
