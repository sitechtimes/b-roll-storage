info:
  name: Create Media
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/medias/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
