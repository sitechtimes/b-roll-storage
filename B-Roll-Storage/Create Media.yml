info:
  name: Create Media
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/medias/"
  body:
    type: json
    data: |-
      {
        "title": "Me when Kenneth does not the rat",
        "type": "image",
        "url": "https://bigrat.kennethng.dev/big-rat-monster.png",
        "tags": ["God", "pigeon", "person"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
