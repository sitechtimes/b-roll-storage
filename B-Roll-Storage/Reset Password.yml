info:
  name: Reset Password
  type: http
  seq: 23

http:
  method: POST
  url: "{{base_url}}/auth/reset-password?token="
  params:
    - name: token
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "password": "horsegooner"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
