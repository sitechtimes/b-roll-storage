info:
  name: Fetch Media
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/medias/"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
