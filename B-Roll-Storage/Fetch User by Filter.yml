info:
  name: Fetch User by Filter
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/users/filter?role=user&name="
  params:
    - name: role
      value: user
      type: query
    - name: name
      value: ""
      type: query
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
